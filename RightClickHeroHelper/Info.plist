<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDisplayName</key>
    <string>RightClickHeroHelper</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>LSMinimumSystemVersion</key>
    <string>$(MACOSX_DEPLOYMENT_TARGET)</string>

    <!-- Background-only login item: no Dock icon, no menu bar -->
    <key>LSBackgroundOnly</key>
    <true/>
    <key>LSUIElement</key>
    <true/>

    <!-- Runtime configuration consumed by RightClickHeroKit.BundleConfig -->
    <key>AppGroupIdentifier</key>
    <string>group.$(BUNDLE_PREFIX).rightclickhero</string>
    <key>HelperBundleIdentifier</key>
    <string>$(BUNDLE_PREFIX).RightClickHero.Helper</string>
    <key>HelperMachServiceName</key>
    <string>$(BUNDLE_PREFIX).RightClickHero.Helper</string>
    <key>AirDropNotificationName</key>
    <string>$(BUNDLE_PREFIX).rightclickhero.showAirDrop</string>

    <!-- Mach service exposed to the Finder Extension over XPC -->
    <key>MachServices</key>
    <dict>
        <key>$(BUNDLE_PREFIX).RightClickHero.Helper</key>
        <true/>
    </dict>
</dict>
</plist>
